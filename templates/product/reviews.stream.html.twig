<turbo-stream action="update" target="product-{{ product.id }}-quick-stats">
    <template>
        {{ include('product/_quickStats.html.twig') }}
    </template>
</turbo-stream>

<turbo-stream action="replace" target="review-{{ product.id }}-list">
    <template>
        {{ include('product/_reviews.html.twig') }}
    </template>
</turbo-stream>